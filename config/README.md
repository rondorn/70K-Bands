# App configuration

Production app configuration shared by Android and iOS.

| Path | Purpose |
|------|---------|
| [festivals/](festivals/) | Per-festival JSON (`70k.json`, `mdf.json`, …) and `registry.json` (FCM topics, share extensions) |

For QA test fixtures and pointer files, see [qa-config/](../qa-config/).
